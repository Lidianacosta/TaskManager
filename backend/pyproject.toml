[project]
name = "taskmanager"
version = "0.1.0"
description = "Add your description here"
readme = "../README.md"
requires-python = ">=3.12"
dependencies = ["fastapi[standard]>=0.136.3", "sqlmodel>=0.0.38"]

[dependency-groups]
dev = ["ruff>=0.15.16"]
